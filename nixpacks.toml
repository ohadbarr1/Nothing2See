[phases.setup]
cmds = ["npm install -g pnpm@9"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = [
  "pnpm --filter @nothing2see/types build",
  "pnpm --filter @nothing2see/api build"
]

[start]
cmd = "node apps/api/dist/server.js"
